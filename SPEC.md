# SPEC.md - FocusFlow Timer

- **Version:** `0.1.0`
- **Status:** `Draft`
- **Description:** 一個簡潔、高效的番茄鐘計時器，旨在幫助使用者專注工作與定時休息。

---

## 1. 產品目標 (Product Goals)

- **[G-1] 提升專注:** 提供一個無干擾的計時環境，幫助使用者進入心流狀態。
- **[G-2] 促進休息:** 透過自動化的休息提醒，避免使用者因過度工作而產生耗竭感。
- **[G-3] 簡潔易用:** 提供一個直觀、無需學習即可上手的操作介面。

---

## 2. 功能性需求 (Functional Requirements - FR)

### FR-001: 計時器核心邏輯 (Timer Core Logic)

系統必須能夠在不同模式（專注、短休息、長休息）下準確倒數計時。

- **AC-1.1: 準確倒數**
  - **Given** 計時器在任意模式下，設定時間為 `N` 秒。
  - **When** 計時器啟動並經過 `M` 秒。
  - **Then** 剩餘時間應為 `N - M` 秒。

- **AC-1.2: 時間歸零停止**
  - **Given** 計時器正在運行且剩餘時間為 1 秒。
  - **When** 經過 1 秒。
  - **Then** 剩餘時間應為 0，且計時器狀態應變為「非活躍 (inactive)」。

### FR-002: 計時器控制 (Timer Controls)

使用者必須能夠控制計時器的啟動、暫停和重置。

- **AC-2.1: 啟動計時器**
  - **Given** 計時器處於「非活躍」狀態。
  - **When** 使用者點擊「Start」按鈕。
  - **Then** 計時器狀態應變為「活躍 (active)」，並開始倒數。

- **AC-2.2: 暫停計時器**
  - **Given** 計時器處於「活躍」狀態。
  - **When** 使用者點擊「Pause」按鈕。
  - **Then** 計時器狀態應變為「非活躍」，並停在當前剩餘時間。

- **AC-2.3: 重置計時器**
  - **Given** 計時器正在倒數或已暫停。
  - **When** 使用者點擊「Reset」按鈕。
  - **Then** 計時器狀態應變為「非活躍」，且剩餘時間應重置為當前模式的初始值。

### FR-003: 番茄鐘週期管理 (Pomodoro Cycle Management)

系統必須能夠自動管理「專注-休息」的週期性流程。

- **AC-3.1: 自動切換至短休息**
  - **Given** 使用者已完成的「專注」週期數為 0, 1, 或 2。
  - **When** 當前的「專注」計時結束。
  - **Then** 系統應自動進入「短休息」模式，並重置計時器為短休息的初始時間。

- **AC-3.2: 自動切換至長休息**
  - **Given** 使用者已連續完成 3 個「專注」週期。
  - **When** 第 4 個「專注」計時結束。
  - **Then** 系統應自動進入「長休息」模式，並重置計時器為長休息的初始時間。

- **AC-3.3: 自動從休息切換回專注**
  - **Given** 使用者正在進行「短休息」或「長休息」。
  - **When** 休息計時結束。
  - **Then** 系統應自動進入「專注」模式，並重置計時器為專注的初始時間。

### FR-004: 使用者回饋 (User Feedback)

系統必須提供清晰的視覺與聽覺回饋，告知使用者當前狀態。

- **AC-4.1: 動態網頁標題**
  - **Given** 計時器正在運行。
  - **When** 剩餘時間更新。
  - **Then** 瀏覽器的標題應格式化為 `MM:SS - [模式名稱]`。

- **AC-4.2: 週期結束提示音**
  - **Given** 任何模式的計時器正在運行。
  - **When** 剩餘時間變為 0。
  - **Then** 系統應播放一次提示音效。

---

## 3. 非功能性需求 (Non-Functional Requirements - NFR)

- **[NFR-1] 性能:** 計時器在 25 分鐘的週期內，誤差不得超過 1 秒。
- **[NFR-2] 相容性:** 應用必須在最新版的 Chrome, Firefox, Safari 和 Edge 瀏覽器上正常運作。
- **[NFR-3] 無障礙:** UI 元件（特別是按鈕）必須有適當的 ARIA 屬性。

---

## 4. 超出範圍 (Out of Scope for v0.1.0)

- 使用者帳號與登入系統。
- 跨裝置同步。
- 自訂計時器時間長度。
- 任務列表 (To-do list) 功能。
- 歷史數據統計與報告。

##

